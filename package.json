{
  "name": "diablo-push-up",
  "version": "1.0.0",
  "description": "Push-up training gamified with Diablo 1 theme",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "node server/index.js",
    "dev": "concurrently \"nodemon server/index.js\" \"npm --prefix client run dev\"",
    "install:all": "npm install && npm --prefix server install; npm --prefix client install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
